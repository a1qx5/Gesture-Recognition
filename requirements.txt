# Core dependencies for Gesture Recognition project
# Note: mediapipe >0.10.14 uses a different API (tasks instead of solutions)
# For Python 3.13+, you must use mediapipe>=0.10.30, but code needs updating
# For Python 3.11-3.12, use mediapipe==0.10.14 for the old solutions API
opencv-python>=4.8.0
mediapipe==0.10.14
numpy>=1.24.0
pandas>=2.0.0

# For future model training (Module 2)
scikit-learn>=1.3.0

PyAutoGUI~=0.9.54
pynput~=1.7.6
pycaw>=20230407
comtypes>=1.2.0
matplotlib~=3.10.8